{
  "name": "rabbitmq",
  "version": "1.0.0",
  "description": "Project for RabbitMQ lab",
  "main": "index.ts",
  "scripts": {
    "compile": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:run": "docker run -it --rm --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:3.13-management"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "amqplib": "^0.10.4"
  }
}
